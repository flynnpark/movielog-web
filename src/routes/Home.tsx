import React from 'react';
import Button from 'components/base/button';

const Home: React.FC = () => {
  return (
    <div>
      Home <Button>Button</Button>
    </div>
  );
};

export default Home;
